- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.gdbinit:
    ~/.gitconfig:
    ~/.inputrc:
    ~/.tmux.conf:
    ~/bin:
